# Active Context

## Current focus
Complete the Vite conversion.

## Recent changes
- Added multi-page HTML entries for Vite builds (config/live config/mobile/video overlay).
- Fixed Jest setup file path to match the .jsx file.
- Removed `vite-plugin-commonjs` and adjusted `define` values; build now succeeds.

## Next steps
- Verify dev server, build output, and tests.
- Confirm extension URLs map to the new HTML entry points.
